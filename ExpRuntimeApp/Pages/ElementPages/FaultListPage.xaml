<Page x:Class="ExpRuntimeApp.Pages.ElementPages.FaultListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ExpRuntimeApp.Pages.ElementPages"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="FaultListPage"
      >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Margin="3" VerticalAlignment="Center"
               Content="本次实验故障："></Label>

        <StackPanel Grid.Row="1" Margin="3">
            <DataGrid AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="类型"/>
                    <DataGridTextColumn Header="时间"/>
                    <DataGridTextColumn Header="名称"/>
                    <DataGridTextColumn Header="定位"/>
                    <DataGridTextColumn Header="状态"/>
                    <DataGridTextColumn Header="处理"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</Page>
