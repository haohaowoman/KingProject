<UserControl x:Class="ExpRuntimeApp.UserControls.HS_ProductInforCtrl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExpRuntimeApp.UserControls"
             xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" 
             DataContext="{StaticResource ExpViewModule}"
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Margin="1" HorizontalAlignment="Stretch">
        <TextBlock Margin="3" VerticalAlignment="Center" Text="产品型号：" HorizontalAlignment="Left"/>
        <TextBox Margin="3" VerticalAlignment="Center" HorizontalAlignment="Stretch" MinWidth="100"
                 Text="{Binding Path=ProductType}"/>
        <TextBlock Margin="3" VerticalAlignment="Center" Text="产品编号：" HorizontalAlignment="Left"/>
        <TextBox Margin="3" VerticalAlignment="Center" HorizontalAlignment="Stretch" MinWidth="100"
                 Text="{Binding Path=ProductID}"/>
    </StackPanel>
</UserControl>
